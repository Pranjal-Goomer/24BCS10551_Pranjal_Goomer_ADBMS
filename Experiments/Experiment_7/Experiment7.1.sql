CREATE TABLE EMPLOYEE (
  SALARY NUMBER PRIMARY KEY,
  name VARCHAR2(15) NOT NULL,
  dept VARCHAR2(10) NOT NULL,
  DEPARTMENT_ID INT NOT NULL
);

INSERT INTO EMPLOYEE VALUES (100, 'Clark', 'Sales', 10);
INSERT INTO EMPLOYEE VALUES (200, 'Dave', 'Accounting', 11);
INSERT INTO EMPLOYEE VALUES (300, 'Ava', 'Sales', 12);

SELECT * 
FROM EMPLOYEE 
WHERE dept = 'Sales';


BEGIN 
  UPDATE EMPLOYEE
  SET SALARY = SALARY * 1.1
  WHERE DEPARTMENT_ID = 10;

  IF SQL%ROWCOUNT > 0 THEN 
    DBMS_OUTPUT.PUT_LINE(SQL%ROWCOUNT || ' ROWS UPDATED');
  ELSE
    DBMS_OUTPUT.PUT_LINE('NO ROWS UPDATED');
  END IF;
END;
/

SELECT * 
FROM EMPLOYEE 
WHERE dept = 'Sales';
